export const firestoreInitialLoadTimeoutMs = 8000

export function firestoreInitialLoadTimeoutMessage() {
  return 'Firebase is taking longer than expected to respond. Check the network connection and try again.'
}
